import * as assert from 'uvu/assert';

import type { Scale } from '../..';
import { modulate } from '../../utils';
import { suite } from 'uvu';

const Modulate = suite('modulate()');

Modulate('TODO', () => {
	console.log(modulate);
	throw new Error('not yetn implemented');
});

Modulate.run();
